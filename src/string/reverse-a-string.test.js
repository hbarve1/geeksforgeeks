const reverseString = require("./reverse-a-string");

describe("", () => {
  test("", () => {
    expect(reverseString("HimankBarve")).toBe("evraBknamiH");
    expect(reverseString("HoneyBarve")).toBe("evraByenoH");
  });
});
