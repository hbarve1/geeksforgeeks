const printFirstLetter = require("./print-first-letter-of-every-word-in-string");

describe("", () => {
  test("", () => {
    expect(printFirstLetter("geeks for geeks")).toBe("gfg");
    expect(printFirstLetter("bad is good")).toBe("big");
  });
});
