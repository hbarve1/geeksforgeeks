const mergeAndReverse = require("./java-trings-set-1");

describe("", () => {
  test("", () => {
    expect(mergeAndReverse("Himank", "Barve")).toBe("evraBknamiH");
    expect(mergeAndReverse("Honey", "Barve")).toBe("evraByenoH");
  });
});
