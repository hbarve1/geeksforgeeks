const countCamelCase = require("./count-of-camel-case-characters");

describe("", () => {
  test("", () => {
    expect(countCamelCase("ckjkUUYII")).toBe(5);
    expect(countCamelCase("abcd")).toBe(0);
  });
});
